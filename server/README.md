# Server

Backend for the Swing app. Not implemented yet.

Planned stack:

- Firebase Authentication (phone OTP)
- Firestore (users, planes, chats, reports)
- Cloud Functions (Node.js) for plane matching, expiry, and moderation
- Firebase Cloud Messaging for push notifications

Once we start backend work, this folder will contain the Cloud Functions source, Firestore security rules, and indexes.
